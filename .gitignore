# Build output
/bin/
/dist/
/mm
/mm.exe

# Local databases and scan artifacts
*.db
*.db-wal
*.db-shm

# Editor / OS
.DS_Store
*.swp

# UI dependencies. The *built* output under internal/webui/dist is committed on
# purpose: a distributable binary has to build without a Node toolchain.
web/node_modules/
web/tsconfig.tsbuildinfo

# Scratch artifacts from local testing. These have leaked into a commit once
# already, when a stray working directory sent a heredoc into web/.
*.log
shot.mjs
holdopen.mjs
